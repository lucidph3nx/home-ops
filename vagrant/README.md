# Vagrant test machines to use for k3s provisioning

### install vagrant
`sudo pacman -S vagrant`

### install libvirt plugin
`vagrant plugin install vagrant-libvirt`

### create machines
`vagrant up`
